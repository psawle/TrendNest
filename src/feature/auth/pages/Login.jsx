import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { useDispatch } from "react-redux"; import { toast } from "react-toastify"; import Navbar from "../../../components/layout/Navbar"; import Input from "../../../components/common/Input"; import Button from "../../../components/common/Button"; import { loginSchema } from "../../../schemas/loginSchema"; import { loginUser } from "../../../store/actions/userActions";
const Login = () => { const navigate=useNavigate(); const dispatch=useDispatch(); const [show,setShow]=useState(false); const {register,handleSubmit,formState:{errors,isSubmitting}}=useForm({resolver:zodResolver(loginSchema)}); const onSubmit=async(data)=>{try{await dispatch(loginUser(data));navigate("/dashboard");}catch(error){toast.error(error.message||"Invalid email or password");}}; return <div className="site-shell"><Navbar/><main className="container-custom grid min-h-[calc(100vh-72px)] items-stretch py-5 lg:grid-cols-[1fr_.9fr]"><aside className="relative hidden overflow-hidden rounded-l-[18px] lg:block"><img src="https://images.unsplash.com/photo-1445205170230-053b83016050?auto=format&fit=crop&w=1200&q=85" alt="Style collection" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-[#171522]/80 via-[#171522]/10 to-transparent"/><div className="absolute inset-x-10 bottom-10 text-white"><p className="text-xs font-bold uppercase tracking-[.16em] text-white/70">Better browsing begins here</p><h1 className="mt-3 text-4xl font-semibold leading-tight">Your next favorite thing is waiting.</h1><p className="mt-4 max-w-md text-sm leading-6 text-white/75">Keep your finds, track your orders, and get first access to fresh drops.</p></div></aside><section className="flex items-center justify-center rounded-[18px] bg-[#f0edff] px-5 py-12 lg:rounded-l-none"><div className="w-full max-w-md rounded-[14px] border border-white/70 bg-white/85 p-6 shadow-[0_16px_50px_rgba(50,35,93,.08)] backdrop-blur sm:p-9"><p className="eyebrow">Welcome back</p><h2 className="mt-3 text-3xl font-semibold">A lovely place to return to.</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Sign in to see your saved pieces and orders.</p><form onSubmit={handleSubmit(onSubmit)} className="mt-7 space-y-5"><Input label="Email address" type="email" placeholder="you@example.com" error={errors.email?.message} {...register("email")}/><div className="relative"><Input label="Password" type={show?"text":"password"} placeholder="Your password" error={errors.password?.message} {...register("password")}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-[34px] text-xs font-bold text-[var(--primary)]">{show?"Hide":"Show"}</button></div><div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-[var(--muted)]"><input type="checkbox" className="accent-[var(--primary)]"/> Remember me</label><button type="button" className="font-bold text-[var(--primary)]">Forgot password?</button></div><Button type="submit" loading={isSubmitting} className="bg-[var(--primary)]">Sign in</Button></form><div className="my-6 flex items-center gap-3 text-xs text-[var(--muted)]"><span className="h-px flex-1 bg-[var(--line)]"/>or continue with<span className="h-px flex-1 bg-[var(--line)]"/></div><div className="grid grid-cols-2 gap-3"><button type="button" className="button-secondary">Google</button><button type="button" className="button-secondary">Apple</button></div><p className="mt-7 text-center text-sm text-[var(--muted)]">New to TrendNest? <Link to="/register" className="font-bold text-[var(--primary)]">Create an account</Link></p></div></section></main></div>}; export default Login;
